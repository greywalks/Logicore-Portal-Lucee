name: Lucee smoke test

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up CommandBox
        uses: Ortus-Solutions/setup-commandbox@v2.0.1
        with:
          installSystemModules: true

      - name: Start Lucee
        run: |
          box server start --noOpenBrowser
          sleep 15

      - name: Verify health endpoint
        run: |
          curl --fail-with-body --show-error --retry 3 --retry-delay 2 http://127.0.0.1:5000/healthz

      - name: Verify login page
        run: |
          curl --fail-with-body --show-error http://127.0.0.1:5000/login | grep -q "Sign in"

      - name: Show Lucee logs on failure
        if: failure()
        run: |
          box server status || true
          echo "--- repository logs ---"
          find . -maxdepth 6 -type f \( -name '*.log' -o -name '*.out' \) -print -exec tail -n 180 {} \; || true
          echo "--- CommandBox / Lucee logs ---"
          find "$HOME/.CommandBox/server" -type f \( -name '*.log' -o -name '*.out' \) -print -exec tail -n 220 {} \; || true

      - name: Stop Lucee
        if: always()
        run: box server stop || true
